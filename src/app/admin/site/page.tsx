import Image from 'next/image';
import { updateSiteSettings } from '@/app/admin/actions';
import { getStoreSettings } from '@/lib/repositories/settings.repository';

export default async function SiteSettingsPage() {
  const settings = await getStoreSettings();
  return <div className="admin-page"><header className="admin-heading"><div><p>CONTENU DU SITE</p><h1>Accueil & réseaux.</h1><span>Change les images que les clients voient en premier, puis les liens de la boutique.</span></div></header><form action={updateSiteSettings} className="site-settings-stack"><section className="admin-panel site-settings-panel"><div className="admin-panel-title"><div><p>PAGE D’ACCUEIL</p><h2>Les grands visuels</h2></div></div><div className="site-settings-form"><div className="settings-image"><Image src={settings.heroImage} alt="Visuel accueil actuel" fill sizes="(max-width: 900px) 100vw, 400px"/><span>Image principale de l’accueil</span></div><label>Remplacer l’image principale<input type="file" name="hero_image" accept="image/png,image/jpeg,image/webp"/></label><input type="hidden" name="hero_image_url" value={settings.heroImage}/><div className="settings-image"><Image src={settings.loungeImage} alt="Visuel secondaire actuel" fill sizes="(max-width: 900px) 100vw, 400px"/><span>Image secondaire de l’accueil</span></div><label>Remplacer l’image secondaire<input type="file" name="lounge_image" accept="image/png,image/jpeg,image/webp"/></label><input type="hidden" name="lounge_image_url" value={settings.loungeImage}/></div></section><section className="admin-panel site-settings-panel"><div className="admin-panel-title"><div><p>CONTACT & RÉSEAUX</p><h2>La boutique partout</h2></div></div><div className="site-settings-form"><label>Nom de la boutique<input name="site_name" defaultValue={settings.siteName}/></label><label>Numéro WhatsApp<input name="whatsapp_phone" defaultValue={settings.whatsappPhone} placeholder="+229 97338843"/></label><label>Instagram<input name="instagram_url" defaultValue={settings.instagramUrl}/></label><label>TikTok<input name="tiktok_url" defaultValue={settings.tiktokUrl}/></label><label className="field-wide">Message WhatsApp par défaut<textarea name="whatsapp_message" rows={4} defaultValue={settings.whatsappDefaultMessage}/></label><button className="admin-primary-button">Publier les changements</button></div></section></form></div>;
}
